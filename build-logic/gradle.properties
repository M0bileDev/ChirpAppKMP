# setup parallel build (makes build faster)
org.gradle.parallel=true
# use cache for what already has built
org.gradle.caching=true
# configure only what is really being referenced and used
org.gradle.configureondemand=true